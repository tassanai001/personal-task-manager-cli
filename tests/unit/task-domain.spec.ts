import { describe, test } from 'vitest';

describe('task domain', () => {
  test('validates priority, status transitions, and ids', async () => {
    throw new Error('Task domain spec not implemented yet.');
  });
});
